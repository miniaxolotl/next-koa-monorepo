{
  "name": "@libs/shared",
  "version": "0.0.1",
  "license": "GPL-3.0",
  "author": "Elias Mawa <elias@emawa.io>",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "scripts": {
    "clean": "echo 'clean @libs/shared' && rm -rf ./dist**",
    "watch": "echo 'watching @libs/shared' && nodemon --watch 'src/' -e ts,tsx --exec yarn build",
    "build": "echo 'building @libs/shared' && tsc",
    "test": "jest"
  }
}
